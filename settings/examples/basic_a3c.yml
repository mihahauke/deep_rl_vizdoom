config_file: basic.cfg
scenario_tag: basic
reward_scale: 0.01

epochs: 20
local_steps_per_epoch: 4000
test_episodes_per_epoch: 100

initial_learning_rate: 0.00008
final_learning_rate: 0.00001
learning_rate_decay_steps: 1e05

initial_entropy_beta: 0.05
final_entropy_beta: 0.0
learning_rate_decay_steps: 1e5

run_tag: example
use_misc: false